# dev-quotes-homepage

This adds a Quotes Card component to your Homepage! It will display a random programmer themed quote on every reload.

Simply add the `QuoteCard` component anywhere you'd like in your Backstage environment!
