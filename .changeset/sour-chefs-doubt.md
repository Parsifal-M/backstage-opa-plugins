---
'@parsifal-m/backstage-plugin-opa-node': patch
---

Adds a standalone dev setup (dev/index.ts) so the service can be run and tested in isolation without a full Backstage app.

Bumps core Backstage dependencies to 1.50.4.