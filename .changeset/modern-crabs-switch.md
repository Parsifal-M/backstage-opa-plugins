---
'@parsifal-m/backstage-plugin-opa-authz-react': minor
---

Add new frontend system support via ./alpha entry point, exporting opaAuthzApi (ApiBlueprint) alongside the existing hooks and components. Includes a standalone dev app for component development without a running backend.

Bumps core Backstage dependencies to 1.50.4.